// @ts-expect-error (this gets removed by the test runner)
// eslint-disable-next-line semi
import type * as npmTypes from '../../types/index.d.ts';

// @ts-expect-error (this gets removed by the test runner)
// eslint-disable-next-line no-undef,semi
export const metadata: npmTypes.TYPE = DATA;
